const timeouts=new Map;export function debounce(cb,ms,id){const timeout=timeouts.get(id);null!=timeout&&window.clearTimeout(timeout),timeouts.set(id,window.setTimeout(()=>{cb(),timeouts.delete(id)},ms))}